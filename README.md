To run the application we use the next command:

```sh
docker-compose up -d
```

stop the application:

```sh
docker-compose down
```

list running container:

```sh
docker-compose ps
```

access container:

```sh
docker exec -it {container_name} bash
```
